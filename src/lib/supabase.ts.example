import { createClient } from '@supabase/supabase-js';

// ⚠️ ATENÇÃO: Substitua estas variáveis pelas suas credenciais do Supabase
// 
// Como obter:
// 1. Acesse: https://supabase.com/dashboard
// 2. Vá em Settings → API
// 3. Copie a "Project URL" e a chave "anon public"
// 4. Cole abaixo

const supabaseUrl = 'https://pvejdlewrneqnlvjradv.supabase.co'; // ⬅️ SUA URL AQUI
const supabaseAnonKey = 'SUA_CHAVE_ANON_AQUI'; // ⬅️ SUA CHAVE ANON AQUI

export const supabase = createClient(supabaseUrl, supabaseAnonKey);

// Tipos para o banco de dados
export interface TaskRow {
  id: string;
  user_id: string;
  user_name: string;
  title: string;
  priority: 'baixa' | 'media' | 'alta' | 'urgente';
  due_date: string;
  completed: boolean;
  created_at: string;
  updated_at: string;
}

export interface SubtaskRow {
  id: string;
  task_id: string;
  title: string;
  completed: boolean;
  created_at: string;
}

